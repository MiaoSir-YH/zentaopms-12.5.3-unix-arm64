# 禅道 12.5.3 Unix ARM64 使用说明

对应源码：https://github.com/easysoft/zentaopms/releases/tag/zentaopms_12.5.3_20210108

## 1. 产物

| 文件 | 类型 | 说明 |
|------|------|------|
| `ZenTaoPMS.12.5.3.zip` | 源码包 | PHP 源码，架构无关，需自备 Apache/Nginx + PHP + MySQL |
| `ZenTaoPMS.12.5.3.zbox_arm64.tar.gz` | 一键包 | Linux **aarch64** 自包含环境：Apache 2.4.54 + PHP 7.4.33 + MariaDB 10.5.27 + 禅道 12.5.3 |

构建仓库：https://github.com/MiaoSir-YH/zentaopms-12.5.3-unix-arm64

---

## 2. 一键包（推荐，ARM64 Linux）

### 2.1 环境要求

- CPU：64 位 ARM（`aarch64` / `arm64`）
- 系统：Linux，glibc **2.31+**（Ubuntu 20.04、Debian 11、CentOS Stream 9 等）
- 权限：解压与启停需要 root
- 端口：默认 Apache **80**、MariaDB **3306**（可改）
- 必须解压到 **`/opt`**（路径写死在二进制里，解压到别处无法运行）

查看架构：

```sh
uname -m
# 应为 aarch64
```

### 2.2 安装

把 `ZenTaoPMS.12.5.3.zbox_arm64.tar.gz` 拷到机器后：

```sh
# 必须在 /opt 下直接解压，不要先解压再拷贝
cd /opt
tar xzf /path/to/ZenTaoPMS.12.5.3.zbox_arm64.tar.gz
```

解压后目录为 `/opt/zbox/`。

### 2.3 启动 / 停止 / 状态

```sh
/opt/zbox/zbox start
/opt/zbox/zbox stop
/opt/zbox/zbox restart
/opt/zbox/zbox status
/opt/zbox/zbox -h
```

启动成功后浏览器访问：

| 地址 | 说明 |
|------|------|
| `http://<服务器IP>/zentao/` | 禅道开源版（首次进入安装向导） |
| `http://<服务器IP>/` | zbox 欢迎页 |
| `http://<服务器IP>/adminer/` | 数据库管理（需先加账号，见下） |

### 2.4 修改端口

```sh
# Apache 改为 8080，MySQL 改为 3307
/opt/zbox/zbox -ap 8080 -mp 3307
/opt/zbox/zbox restart
```

之后访问 `http://<IP>:8080/zentao/`。

### 2.5 默认账号

| 用途 | 用户名 | 密码 |
|------|--------|------|
| MariaDB root | `root` | `123456` |
| 禅道数据库用户 | `zentao` | `123456` |
| 禅道管理员 | 安装向导里自行设置（官方常见默认 `admin` / `123456`） | |

首次打开 `/zentao/` 会进入安装向导，按提示填写数据库：

- 数据库服务器：`127.0.0.1` 或 `localhost`
- 端口：`3306`（若改过端口则填新端口）
- 用户名 / 密码：`root` / `123456`（或 `zentao` / `123456`）
- 数据库名：`zentao`（向导会创建）

安装完成后删除（或改名）安装脚本：

```sh
rm -f /opt/zbox/app/zentao/www/install.php
rm -f /opt/zbox/app/zentao/www/upgrade.php
```

### 2.6 Adminer（网页管库）

```sh
/opt/zbox/auth/adduser.sh
# 按提示添加 HTTP Basic 用户
```

然后访问 `/adminer/`，数据库填 `localhost:3306`，用户 `root`，密码 `123456`。

### 2.7 常用路径

```
/opt/zbox/
├── zbox                 # 启停脚本
├── BUILD_INFO.txt       # 构建信息
├── app/zentao/          # 禅道程序
│   └── www/             # 网站根（DocumentRoot 别名 /zentao）
├── data/mysql/          # 数据库文件
├── etc/apache/httpd.conf
├── etc/mysql/my.cnf
├── etc/php/php.ini
├── logs/                # apache / mysql / php 日志
├── run/                 # Apache、PHP、MariaDB 二进制
└── tmp/
```

日志：

```sh
tail -f /opt/zbox/logs/apache_error_log
tail -f /opt/zbox/logs/mysql_error.log
tail -f /opt/zbox/logs/php_error_log
```

### 2.8 开机自启（可选）

```sh
cat >/etc/systemd/system/zbox.service <<'EOF'
[Unit]
Description=ZenTao zbox (Apache + MariaDB)
After=network.target

[Service]
Type=forking
ExecStart=/opt/zbox/zbox start
ExecStop=/opt/zbox/zbox stop
Restart=on-failure

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable --now zbox
```

---

## 3. 源码包（自备运行环境）

适用于已有 PHP + MySQL 的任意架构（x86_64 / aarch64 均可）。

### 3.1 环境要求

- PHP **5.3–7.4**（本包按 7.4 验证；不要用 PHP 8）
- 扩展：`pdo`、`pdo_mysql`、`mysqli`、`mbstring`、`json`、`gd`、`curl`、`zip`
- MySQL 5.5+ 或 MariaDB 10.x
- Apache（`mod_php` + `mod_rewrite`）或 Nginx + PHP-FPM
- 目录可写：`tmp/`、`www/data/`、`config/`

### 3.2 安装

```sh
cd /var/www/html
unzip ZenTaoPMS.12.5.3.zip
# 得到 zentaopms/ 目录

chmod -R 777 zentaopms/tmp zentaopms/www/data zentaopms/config
chmod 777 zentaopms/www zentaopms/module
```

Apache 示例：把站点根指到 `zentaopms/www`，或配置别名 `/zentao` → `zentaopms/www`。

浏览器打开：

```
http://<IP>/zentaopms/www/install.php
```

按向导填写数据库后完成安装，然后删除 `www/install.php`。

源码包已带 `www/install.php`（由 `install.php.tmp` 改名），无需再拷。

---

## 4. 故障排查

| 现象 | 处理 |
|------|------|
| 解压后 `php: not found` / 无法启动 | 确认是 **aarch64** Linux，并且解压在 **`/opt/zbox`** |
| Apache 启动失败 | `80` 被占用：`/opt/zbox/zbox -ap 8080`；看 `/opt/zbox/logs/apache_error_log` |
| MySQL 启动失败 | `3306` 被占用：`/opt/zbox/zbox -mp 3307`；看 `/opt/zbox/logs/mysql_error.log` |
| 页面空白 / 500 | 看 PHP 日志；确认 `tmp`、`www/data`、`config` 可写 |
| `uname -m` 为 `x86_64` | 本一键包不能用，请用官方 x86_64 zbox 或源码包 |
| PHP 8 跑源码包报错 | 12.5.3 不支持 PHP 8，请用 7.2/7.4 |

---

## 5. 已知限制

- **未包含** ionCube（仅 x86）
- **未包含** 喧喧 `xxd` 守护进程（仅 x86），IM 相关能力不可用
- 该 Git 标签未提交 `XUANPATH`，源码包只合并了仓库内 `xuanxuan/` 目录，不是官方 `make pms` 的完整喧喧归档
- 一键包面向 glibc 2.31+ 的 ARM64 Linux，不支持 Android、macOS、Windows

---

## 6. 重新构建

```sh
git clone https://github.com/MiaoSir-YH/zentaopms-12.5.3-unix-arm64.git
cd zentaopms-12.5.3-unix-arm64
gh workflow run build.yml
gh run watch
```

Actions 使用 `ubuntu-24.04-arm`，产物在对应 run 的 Artifacts 中。
